{
  "name": "binarytester",
  "version": "1.0.0",
  "description": "",
  "main": "binaryTester.js",
  "type": "module",
  "scripts": {
    "build": "echo 'Compilation' && npm i && tsc && cp dist/* . -r && cat src/shebang > binaryTester && cat binaryTester.js >> binaryTester && chmod +x binaryTester && rm -rf dist",
    "clean": "echo 'Cleaning' && rm $(find ./src/ -name '*.js') && rm binaryTester.js binaryTester"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "i": "^0.3.7",
    "js-yaml": "^4.1.0",
    "npm": "^9.6.2",
    "process": "^0.11.10",
    "typescript": "^5.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.15.3"
  }
}
