{
  "name": "binarytester",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "echo 'Compilation' && tsc && cp dist/* . -r",
    "clean": "echo 'Cleaning' && rm $(find ./src/ -name '*.js') && rm index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "js-yaml": "^4.1.0",
    "process": "^0.11.10"
  },
  "devDependencies": {
    "@types/node": "^18.15.3"
  }
}
